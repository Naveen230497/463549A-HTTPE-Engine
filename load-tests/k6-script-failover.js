import http from 'k6/http'; import { check, sleep } from 'k6'; export const options = { vus: 12000, duration: '20m', }; export default function () { const payload = JSON.stringify({ amount: 25.0, currency: 'INR', destination: 'uuid-here' }); const params = { headers: { 'Content-Type': 'application/json', 'X-Idempotency-Key': 'failover-' + __VU + '-' + __ITER } }; const res = http.post('http://api.gateway.local/v1/payments/transfer', payload, params); check(res, { 'status is 201': (r) => r.status === 201, 'status is 503 (during failover)': (r) => r.status === 503 }); sleep(1); }
